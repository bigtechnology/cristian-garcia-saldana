import React from 'react';
import RenderGalleryPage from './RenderGalleryPage';

const GalleryPageContainer = (): React.ReactElement => {
  return (
    <div>
      <RenderGalleryPage />
    </div>
  );
};

export default GalleryPageContainer;
